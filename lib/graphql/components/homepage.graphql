# Example of GraphQL query
# Query is read from every folder of project
# Its necessary create file with end .graphql --> *.graphql
query Homepage {
  homepage(locale:"cs") {
    id
    h1_perex
    headline_1
    locale
  }
}